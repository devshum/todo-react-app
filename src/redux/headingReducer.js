const initialState = {
    date: {
        day: 'Monday',
        month: 'Oct',
        year: 2020,
        date: 9 ,
    }
};

const headingReducer = (state = initialState, action) => {
    return state;
};

export default headingReducer;